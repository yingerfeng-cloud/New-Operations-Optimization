import { validateFormula } from '../../features/formula-editor/formulaValidator';
test('accepts linear aggregate constraint',()=>expect(validateFormula('sum(p[u,t] for u in unit) >= load[t]','constraint').valid).toBe(true));
test('rejects unsupported relation',()=>expect(validateFormula('p[t] != load[t]','constraint').valid).toBe(false));
test('validates referenced objects, indices, and foreach scope',()=>{const symbols={sets:{unit:'机组',time:'时段'},parameters:{load:{label:'负荷',indices:['time']}},variables:{p:{label:'出力',indices:['unit','time']}}};const tokens=[{type:'variable' as const,code:'p',label:'出力',indices:['unit','time'],indexAliases:['u','t']},{type:'operator' as const,code:'<=',label:'<='},{type:'parameter' as const,code:'load',label:'负荷',indices:['time'],indexAliases:['t']}];expect(validateFormula('p[u,t] <= load[t]','constraint',tokens,symbols,['unit','time']).valid).toBe(true);expect(validateFormula('p[u,t] <= load[t]','constraint',tokens,symbols,['time']).errors).toContain('自由索引未被作用范围覆盖：unit')});
test('rejects missing context objects',()=>{const tokens=[{type:'variable' as const,code:'missing_p',label:'缺失变量',indices:['time'],indexAliases:['t']}];expect(validateFormula('missing_p[t] <= 1','constraint',tokens,{sets:{time:'时段'},parameters:{},variables:{}},['time']).errors).toContain('引用变量不存在：missing_p')});
