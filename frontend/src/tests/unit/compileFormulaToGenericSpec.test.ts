import { compileFormulaToGenericSpec } from '../../features/model-creation/utils/compileFormulaToGenericSpec'; import type { FormulaDef } from '../../types/formula';
const base=(kind:'constraint'|'objective',dsl:string):FormulaDef=>({formula_id:dsl,name:dsl,kind,display_formula:dsl,dsl_formula:dsl,tokens:[],foreach:[],referenced_sets:[],referenced_parameters:[],referenced_variables:[],free_indices:kind==='constraint'?['time']:[],compile_status:'ready'});const semantic={sets:[{code:'unit',values:['u1']},{code:'time',values:[0]}],parameters:[{code:'load',default:[10]},{code:'cost',default:{u1:1}}],variables:[{code:'p',dimension:['unit','time'],domain:'NonNegativeReals'}]};
test('compiles required minimal LP',()=>{const spec=compileFormulaToGenericSpec([base('constraint','sum(p[u,t] for u in unit) >= load[t]'),base('objective','sum(cost[u] * p[u,t] for u in unit for t in time)')],semantic);expect(spec.constraints[0]).toMatchObject({sense:'>=',rhs_param:'load'});expect(spec.objective.terms[0]).toMatchObject({var:'p',coef_param:'cost',foreach:['unit','time']})});
