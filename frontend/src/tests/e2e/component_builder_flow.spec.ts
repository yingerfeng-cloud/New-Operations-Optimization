import {test,expect} from '@playwright/test';import {mockApi} from './fixtures';test('component builder can be selected',async({page})=>{await mockApi(page);await page.goto('/models/create?scenarioId=cascade_hydro_day_ahead&modelId=cascade_hydro_dispatch_lp');await expect(page.getByTestId('builder-mode-select')).toContainText('组件化 Builder');await expect(page.getByText(/组件能力自动汇总/)).toBeVisible()});
