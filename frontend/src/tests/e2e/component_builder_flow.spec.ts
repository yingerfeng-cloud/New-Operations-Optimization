import {test,expect} from '@playwright/test';import {mockApi} from './fixtures';test('component builder can be selected',async({page})=>{await mockApi(page);await page.goto('/models/create');await page.getByText('组件化 Builder').click();await expect(page.getByText(/组件能力自动汇总/)).toBeVisible()});
