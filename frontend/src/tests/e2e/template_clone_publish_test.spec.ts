import {test,expect} from '@playwright/test';import {mockApi} from './fixtures';test('template selection starts five-step flow',async({page})=>{await mockApi(page);await page.goto('/models/create');await expect(page.getByText('模型创建').first()).toBeVisible();await expect(page.getByText('模板选择')).toBeVisible();await expect(page.getByText('校验发布')).toBeVisible()});
