import {test,expect} from '@playwright/test';import {mockApi} from './fixtures';test('generic builder is the default creation mode',async({page})=>{await mockApi(page);await page.goto('/models/create');await expect(page.getByTestId('builder-mode-select')).toContainText('通用线性 Builder');await expect(page.getByRole('button',{name:/数学展开/})).toBeVisible()});
