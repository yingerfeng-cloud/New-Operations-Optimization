import {test,expect} from '@playwright/test';import {mockApi} from './fixtures';test('result center renders report entry',async({page})=>{await mockApi(page);await page.goto('/results');await expect(page.getByText('结果报告库').first()).toBeVisible();await expect(page.getByText('导出报告（预留）')).toBeVisible()});
