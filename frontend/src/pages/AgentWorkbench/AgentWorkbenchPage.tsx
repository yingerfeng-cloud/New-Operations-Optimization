import { Card, Col, Input, List, Row, Select, Space, Tag, Button, Timeline } from 'antd'; import { useState } from 'react';
export function AgentWorkbenchPage(){const [messages,setMessages]=useState<string[]>([]);const [text,setText]=useState('');return <Row gutter={16}><Col xs={24} lg={16}><Card title="Agent 对话" extra={<Tag color="orange">API Key 未配置</Tag>}><List style={{minHeight:300}} dataSource={messages} locale={{emptyText:'输入优化需求开始对话'}} renderItem={m=><List.Item>{m}</List.Item>}/><Space.Compact block><Input value={text} onChange={e=>setText(e.target.value)} placeholder="描述您的优化任务"/><Button type="primary" onClick={()=>{if(text){setMessages(x=>[...x,text]);setText('')}}}>发送</Button></Space.Compact></Card></Col><Col xs={24} lg={8}><Card title="调用上下文"><Select style={{width:'100%'}} placeholder="选择模型" options={[]}/><h4>Skill 列表</h4><Tag>模型诊断</Tag><Tag>参数抽取</Tag><h4>调用日志</h4><Timeline items={[{children:'等待调用'}]}/></Card></Col></Row>}
