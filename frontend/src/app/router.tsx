import { createBrowserRouter } from 'react-router-dom'; import { MainLayout } from './layout/MainLayout'; import { DashboardPage } from '../pages/Dashboard/DashboardPage'; import { SettingsPage } from '../pages/Settings/SettingsPage'; import { AgentWorkbenchPage } from '../pages/AgentWorkbench/AgentWorkbenchPage';
const pending=(name:string)=>(()=><div>{name}正在迁移</div>); const ModelCenter=pending('模型资产中心'); const ModelCreation=pending('模型创建'); const ComponentLibrary=pending('组件库'); const TaskCenter=pending('任务中心'); const ResultCenter=pending('结果中心');
export const router=createBrowserRouter([{path:'/',element:<MainLayout/>,children:[{index:true,element:<DashboardPage/>},{path:'models',element:<ModelCenter/>},{path:'models/create',element:<ModelCreation/>},{path:'models/:id',element:<ModelCenter/>},{path:'components',element:<ComponentLibrary/>},{path:'components/:id',element:<ComponentLibrary/>},{path:'tasks',element:<TaskCenter/>},{path:'results',element:<ResultCenter/>},{path:'agents',element:<AgentWorkbenchPage/>},{path:'settings',element:<SettingsPage/>}]}]);
