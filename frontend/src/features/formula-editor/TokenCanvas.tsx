import type { FormulaToken } from '../../types/formula'; import { tokenToDisplay } from './formulaDsl'; import { AggregateBlock } from './AggregateBlock'; export function TokenCanvas({tokens,onRemove}:{tokens:FormulaToken[];onRemove?:(i:number)=>void}){return <div className="token-canvas" data-testid="token-canvas">{tokens.length===0&&<span style={{color:'#8c8c8c'}}>从工具栏插入变量、参数、集合、聚合块或运算符</span>}{tokens.map((t,i)=>t.type==='aggregate'?<AggregateBlock token={t} key={i}/>:<button type="button" className="formula-token" key={i} onClick={()=>onRemove?.(i)}>{tokenToDisplay(t)}</button>)}</div>}
