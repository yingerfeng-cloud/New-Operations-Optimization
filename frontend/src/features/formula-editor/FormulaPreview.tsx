import { Descriptions } from 'antd'; import type { FormulaDef } from '../../types/formula'; export function FormulaPreview({formula}:{formula:FormulaDef}){return <Descriptions size="small" column={1} items={[{key:'display',label:'中文公式',children:formula.display_formula||'-'},{key:'dsl',label:'底层 DSL',children:<code>{formula.dsl_formula||'-'}</code>},{key:'foreach',label:'外层 foreach',children:formula.foreach.join(', ')||'-'},{key:'refs',label:'引用对象',children:[...formula.referenced_variables,...formula.referenced_parameters].join(', ')||'-'}]}/>}
