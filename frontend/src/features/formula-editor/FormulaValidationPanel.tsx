import { Alert, List } from 'antd'; import type { FormulaValidation } from './formulaValidator'; export function FormulaValidationPanel({result}:{result:FormulaValidation}){return <Alert type={result.valid?'success':'error'} showIcon message={result.valid?'公式校验通过':'公式校验失败'} description={<List size="small" dataSource={[...result.errors,...result.warnings]} renderItem={x=><List.Item>{x}</List.Item>}/>} />}
