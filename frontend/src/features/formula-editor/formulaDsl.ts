import type { FormulaToken } from '../../types/formula';
const opLabel:Record<string,string>={'>=':'≥','<=':'≤','==':'=','=':'=','!=':'≠','+':'+','-':'−','*':'×','/':'÷'};
export function tokenToDsl(token:FormulaToken):string{if(token.type==='aggregate')return `${token.fn}(${tokensToDsl(token.bodyTokens)} for ${token.alias} in ${token.setCode})`;if(token.type==='function')return `${token.fn}(${tokensToDsl(token.bodyTokens)})`;if(token.type==='operator')return token.code==='='?'==':token.code;if(token.type==='number')return String(token.value);const aliases=token.indexAliases||token.indices||[];return `${token.code}${aliases.length?`[${aliases.join(',')}]`:''}`}
export function tokenToDisplay(token:FormulaToken):string{if(token.type==='aggregate')return `${token.fn==='sum'?'Σ':token.fn}(${token.setCode} ${token.alias})：[${tokensToDisplay(token.bodyTokens)}]`;if(token.type==='function')return `${token.fn}(${tokensToDisplay(token.bodyTokens)})`;if(token.type==='operator')return opLabel[token.code]||token.code;if(token.type==='number')return String(token.value);const aliases=token.indexAliases||token.indices||[];return `${token.label||token.code}${aliases.length?`[${aliases.join(',')}]`:''}`}
export const tokensToDsl=(tokens:FormulaToken[])=>tokens.map(tokenToDsl).join(' ').replace(/\s+([\],)])/g,'$1');export const tokensToDisplay=(tokens:FormulaToken[])=>tokens.map(tokenToDisplay).join(' ');
export function collectReferences(tokens:FormulaToken[]){const sets=new Set<string>(),parameters=new Set<string>(),variables=new Set<string>(),indices=new Set<string>(),aggregated=new Set<string>();const visit=(list:FormulaToken[])=>list.forEach(t=>{if(t.type==='aggregate'){sets.add(t.setCode);aggregated.add(t.setCode);visit(t.bodyTokens)}else if(t.type==='function')visit(t.bodyTokens);else if(t.type==='set')sets.add(t.code);else if(t.type==='parameter'){parameters.add(t.code);(t.indices||[]).forEach(x=>indices.add(x))}else if(t.type==='variable'){variables.add(t.code);(t.indices||[]).forEach(x=>indices.add(x))}});visit(tokens);return{referenced_sets:[...sets],referenced_parameters:[...parameters],referenced_variables:[...variables],referenced_indices:[...indices],aggregated_indices:[...aggregated],free_indices:[...indices].filter(x=>!aggregated.has(x))}}
