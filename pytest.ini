[pytest]
addopts = -p no:cacheprovider -m "not slow" -n 4 --dist load
testpaths = tests
norecursedirs = .venv __pycache__ __chrome_frontend_profile .pytest_cache pytest_tmp reports
markers =
    slow: integration-style tests that run solver/agent flows, background fallback paths, or polling loops and are excluded from default CI
